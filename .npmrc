audit=true
audit-level=high
auto-install-peers=true
registry=https://registry.npmjs.org/
save-prefix=
strict-ssl=true
unsafe-perm=false

# npm authentication (token provided via NODE_AUTH_TOKEN env var in CI)
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
