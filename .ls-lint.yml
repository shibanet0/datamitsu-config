# Layers over .datamitsu/ls-lint-managed.yml.
# Redefining .dir replaces the base rule; restate regex:\.[a-z0-9_-]+ for dot-directories.
# ignore adds exclusions to the base.
ignore:
  - dist-inline-eslint-config
ls: {}
